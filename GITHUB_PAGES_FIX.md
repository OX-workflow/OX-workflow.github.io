# GitHub Pages Fix

This release changes deployment strategy:
- Build React/Vite output.
- Publish `dist/public` through GitHub Pages Actions.
- Keep backend/server code separate from static presentation.

ONYX case study is intended to be a public static product presentation.
